import { cookies } from "next/headers";
import { NextResponse } from "next/server";
import { db } from "@/lib/db";
import { escalateChat, getPublicChatbot } from "@/lib/ai/chatbot-service";
import {DevelopmentOutboxProvider} from "@/lib/email-provider";
export async function POST(_:Request,{params}:{params:Promise<{publicId:string}>}){const {publicId}=await params,chatbot=await getPublicChatbot(db,publicId);if(!chatbot)return NextResponse.json({error:"Not found"},{status:404});const token=(await cookies()).get(`sp_chat_${publicId.slice(0,8)}`)?.value;if(!token)return NextResponse.json({error:"Start a chat first"},{status:400});await escalateChat(db,chatbot.id,token);const owners=await db.membership.findMany({where:{workspaceId:chatbot.workspaceId,role:"OWNER"},include:{user:true}}),provider=new DevelopmentOutboxProvider(db);for(const owner of owners){const queued=await provider.queue({workspaceId:chatbot.workspaceId,recipientUserId:owner.user.id,to:owner.user.email,type:"HUMAN_ESCALATION",subject:"Customer requested human support",body:"A public chatbot visitor requested human support. AI replies are suspended for that conversation.",actionUrl:"http://localhost:3000/dashboard/conversations"});await provider.deliver(queued.id)}return NextResponse.json({ok:true})}
