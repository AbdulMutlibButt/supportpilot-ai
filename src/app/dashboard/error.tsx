"use client"; import { AlertTriangle } from "lucide-react"; export default function Error({reset}:{reset:()=>void}){return <main className="page-wrap"><div className="empty-state panel"><div className="icon-box"><AlertTriangle size={21}/></div><h3>We couldn’t load this page</h3><p>Something unexpected happened. Your data is safe—please try loading the page again.</p><button className="btn-primary" onClick={reset}>Try again</button></div></main>}
