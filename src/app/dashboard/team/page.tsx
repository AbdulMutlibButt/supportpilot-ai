import {requireWorkspace} from "@/lib/auth"; import {db} from "@/lib/db"; import {inviteAction} from "@/app/actions/support"; import {PageHead} from "@/components/page-ui";
export default async function Team({searchParams}:{searchParams:Promise<{invite?:string}>}){const {invite}=await searchParams,{workspace,membership}=await requireWorkspace();const [people,pending]=await Promise.all([db.membership.findMany({where:{workspaceId:workspace.id},include:{user:true},orderBy:{createdAt:"asc"}}),db.invitation.findMany({where:{workspaceId:workspace.id,acceptedAt:null,expiresAt:{gt:new Date()}},orderBy:{createdAt:"desc"}})]);const link=invite?`http://localhost:3000/invitations/${invite}`:null;return <main className="page-wrap"><PageHead title="Team" desc={`Manage members and roles for ${workspace.name}.`}/>{membership.role==="OWNER"&&<section className="panel mb-4"><h2>Invite teammate</h2><form action={inviteAction} className="mt-4 flex flex-col gap-3 sm:flex-row"><input className="filter-input flex-1" type="email" name="email" required placeholder="teammate@company.com"/><select className="filter-input" name="role" defaultValue="AGENT"><option>OWNER</option><option>AGENT</option><option>VIEWER</option></select><button className="btn-primary">Create invitation</button></form>{link&&<div className="mt-4 rounded-lg bg-[#6558f512] p-3 text-sm"><b>Development invitation link</b><p className="mt-1 break-all text-[var(--muted)]">{link}</p><p className="mt-1 text-xs text-[var(--muted)]">Share this securely. It expires in seven days and can be used once.</p></div>}</section>}<section className="panel"><div className="panel-head"><h2>Workspace members</h2><span className="panel-sub">{people.length} members</span></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Member</th><th>Email</th><th>Role</th><th>Joined</th></tr></thead><tbody>{people.map(m=><tr key={m.id}><td><div className="flex items-center gap-3"><div className="avatar">{m.user.name.slice(0,2).toUpperCase()}</div><b>{m.user.name}</b></div></td><td>{m.user.email}</td><td><span className="badge">{m.role.toLowerCase()}</span></td><td>{m.createdAt.toLocaleDateString()}</td></tr>)}</tbody></table></div></section>{membership.role==="OWNER"&&<section className="panel mt-4"><div className="panel-head"><h2>Pending invitations</h2></div>{pending.length?pending.map(i=><div className="activity-row" key={i.id}><div><b>{i.email}</b><span>{i.role.toLowerCase()} · expires {i.expiresAt.toLocaleDateString()}</span></div></div>):<p className="panel-sub">No pending invitations.</p>}</section>}</main>}
