import {acceptInviteAction} from "@/app/actions/support"; import {requireUser} from "@/lib/auth";
export default async function InvitationPage({params}:{params:Promise<{token:string}>}){const {token}=await params;const user=await requireUser();return <main className="grid min-h-screen place-items-center p-6"><section className="panel max-w-md text-center"><h1 className="text-2xl font-semibold">Join the workspace</h1><p className="mt-3 text-sm text-[var(--muted)]">You’re signed in as {user.email}. Accept this invitation to add the workspace to your account.</p><form action={acceptInviteAction} className="mt-6"><input type="hidden" name="token" value={token}/><button className="btn-primary w-full">Accept invitation</button></form></section></main>}
