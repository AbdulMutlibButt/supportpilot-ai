"use client"; import Link from "next/link"; import {CheckCircle2,Sparkles} from "lucide-react"; import {useActionState} from "react"; import {login,register,AuthState} from "@/app/actions/auth";
export function AuthPage({mode}:{mode:"login"|"register"}){const reg=mode==="register",[state,action,pending]=useActionState(reg?register:login,{} as AuthState);const err=(n:string)=>state.errors?.[n]?.[0];return <main className="auth-page"><section className="auth-art"><Link href="/" className="brand"><span className="brand-mark"><Sparkles size={18}/></span>SupportPilot <b className="!text-white">AI</b></Link><div className="auth-copy"><h1>{reg?"Build customer loyalty, one conversation at a time.":"Welcome back to calmer customer support."}</h1><p>Everything your team needs to deliver thoughtful, fast support—organized in one beautiful workspace.</p></div><div className="auth-trust"><span><CheckCircle2 size={14}/> Secure sessions</span><span><CheckCircle2 size={14}/> Protected workspace</span></div></section><section className="auth-panel"><form action={action} className="auth-form"><h2>{reg?"Create your workspace":"Welcome back"}</h2><p>{reg?"Start your SupportPilot workspace today.":"Enter your details to access your workspace."}</p>{state.message&&<div className="mt-5 rounded-lg bg-red-50 p-3 text-sm text-red-700">{state.message}</div>}<div className="form-grid">{reg&&<><Field name="name" label="Full name" placeholder="Alex Morgan" error={err("name")}/><Field name="workspace" label="Workspace name" placeholder="Northstar Support" error={err("workspace")}/></>}<Field name="email" label="Work email" placeholder="you@company.com" type="email" error={err("email")}/><Field name="password" label="Password" placeholder={reg?"8+ chars, uppercase and number":"Your password"} type="password" error={err("password")}/><button disabled={pending} className="btn-primary auth-submit" type="submit">{pending?"Please wait…":reg?"Create workspace":"Log in to SupportPilot"}</button></div><p className="form-note">{reg?"Already have an account? ":"New to SupportPilot? "}<Link href={reg?"/login":"/register"}>{reg?"Log in":"Create an account"}</Link></p></form></section></main>}
function Field({name,label,placeholder,type="text",error}:{name:string,label:string,placeholder:string,type?:string,error?:string}){return <div className="field"><label htmlFor={name}>{label}</label><input id={name} name={name} type={type} placeholder={placeholder} autoComplete={name}/>{error&&<p className="mt-1 text-xs text-red-600">{error}</p>}</div>}
