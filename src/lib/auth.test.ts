import {describe,expect,it} from "vitest"; import {compare} from "bcryptjs"; import {registerSchema,loginSchema} from "./validation"; import {belongsToWorkspace,can} from "./authorization";
describe("authentication",()=>{it("rejects weak registration credentials",()=>expect(registerSchema.safeParse({name:"A",workspace:"X",email:"bad",password:"password"}).success).toBe(false));it("normalizes valid email",()=>expect(loginSchema.parse({email:"USER@EXAMPLE.COM",password:"x"}).email).toBe("user@example.com"));it("does not accept a different password hash",async()=>expect(await compare("wrong","$2b$12$0JqLyFH14GOq2kzbhfPLNeZQDB2xyfWZQwLrG3.jB8YrgIMj5BAmq")).toBe(false))});
describe("workspace authorization",()=>{it("enforces role hierarchy",()=>{expect(can("OWNER","AGENT")).toBe(true);expect(can("AGENT","OWNER")).toBe(false);expect(can("VIEWER","AGENT")).toBe(false)});it("prevents cross-workspace access",()=>expect(belongsToWorkspace([{workspaceId:"a"}],"b")).toBe(false))});
