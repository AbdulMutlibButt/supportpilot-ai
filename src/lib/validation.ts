import {z} from "zod"; export const registerSchema=z.object({name:z.string().trim().min(2,"Enter your full name."),workspace:z.string().trim().min(2,"Enter a workspace name."),email:z.email("Enter a valid email.").toLowerCase(),password:z.string().min(8,"Use at least 8 characters.").regex(/[A-Z]/,"Add an uppercase letter.").regex(/[0-9]/,"Add a number.")}); export const loginSchema=z.object({email:z.email("Enter a valid email.").toLowerCase(),password:z.string().min(1,"Enter your password.")});
